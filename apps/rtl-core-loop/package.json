{
  "name": "@rtl-agent/rtl-core-loop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "rtl-core-loop": "./dist/index.js"
  },
  "scripts": {
    "agent:probe": "node dist/index.js agent-probe",
    "compile:smoke": "node dist/index.js compile-smoke",
    "fixtures:check": "node dist/index.js fixtures-check",
    "test": "vitest run apps/rtl-core-loop/test --config vitest.config.ts --root ../.."
  },
  "dependencies": {
    "@rtl-agent/core-loop": "workspace:*"
  }
}
