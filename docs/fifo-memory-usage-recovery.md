# Fixed-v1 usage diagnostic: bounded queue recovery

2026-09-18, after10 attempted samples and before any independent mutant evaluation.
Campaign: fifo-usage-v1-20260918-0931. Original plan/protocol/runtime remain hash-locked.

A-R3 completed two provider turns, both with PASSED provider audits. The second edited
testbench added a second TopModule instance; the unchanged topology guard rejected it with
TARGET_EXTRA_DUT_INSTANCE. That child exited1 before publishing execution.json or the
last-attempt verification snapshot. Original queue stopped and preserved its active lock.
Native read-only process inspection at02:21Z found no experiment/model/compiler process.

This is a generated program violating the fixed target boundary, not a network failure.
The original queue labeled missing execution.json infrastructure-failed. Preserve that raw
record and add an explicit review correction to generation-failed/TARGET_EXTRA_DUT_INSTANCE
in the final seal. Do not hide the discrepancy, remove the sample from denominator3, rerun
A-R3, pick its earlier successful snapshot, or compile the disallowed final topology.

Continue only the two predeclared, not-yet-started conditions D-R3 and B-R3. An independent
recovery script must verify the original plan hash, frozen runtime hashes, Memory and suite,
prior10 records and sealed successful snapshots, exact failed-sample provider/boundary evidence,
and dead lock owner before claiming the existing serialization lock with a new ownership record.
Keep the old lock/failure/audit bytes as evidence. Use identical original target runner commands,
conditions, budgets, baseline and exposure audits. No model feedback about this failure is
passed to either new independent condition. No runtime, guidance, Memory, or protocol editing.

After12 terminal samples, seal11 available finals and one unavailable failure if the last2
complete normally. Independent evaluation runs only available final golden/M010 pairs; A-R3
stays in the denominator with unavailable/failed status. This recovery changes orchestration
and corrects failure classification, not the predeclared intervention or final selection rule.
