// Seeded target fixtures derived from the immutable FIFO v2 publication.
export const DPRETET_TB =
  '`timescale 1ns/1ps\nmodule tb;\n  reg si_clk=0,so_clk=0,si_res_n=0,so_res_n=0,shift_in=0,shift_out=0;\n  reg [7:0] d_in=0;\n  wire [7:0] d_out;\n  wire full,almost_full,empty,almost_empty;\n  TopModule #(.DSIZE(8),.ASIZE(3),.FALLTHROUGH("TRUE")) dut(.wclk(si_clk),.wrst_n(si_res_n),.winc(shift_in),.wdata(d_in),.wfull(full),.awfull(almost_full),.rclk(so_clk),.rrst_n(so_res_n),.rinc(shift_out),.rdata(d_out),.rempty(empty),.arempty(almost_empty));\n  always #5 si_clk=~si_clk;\n  always #7 so_clk=~so_clk;\n  reg [7:0] queue[0:4095];\n  integer head=0,tail=0,reads=0,i,j;\n  task put(input [7:0] value);\n    begin\n      @(negedge si_clk); while(full) @(negedge si_clk);\n      d_in=value;shift_in=1;\n      @(posedge si_clk);queue[tail]=value;tail=tail+1;\n      @(negedge si_clk);shift_in=0;\n    end\n  endtask\n  task get;\n    begin\n      @(negedge so_clk);while(empty) @(negedge so_clk);\n      shift_out=1;\n      @(posedge so_clk);\n      if(d_out !== queue[head]) $fatal(1,"DATA_FAIL index=%0d got=%h expected=%h",head,d_out,queue[head]);\n      head=head+1;reads=reads+1;\n      @(negedge so_clk);shift_out=0;\n    end\n  endtask\n  task reset_fifo;\n    begin\n      shift_in=0;shift_out=0;si_res_n=0;so_res_n=0;\n      repeat(5) @(negedge so_clk);\n      si_res_n=1;so_res_n=1;head=0;tail=0;\n      repeat(5) @(negedge so_clk);\n      if(full || !empty) $fatal(1,"RESET_FLAGS");\n    end\n  endtask\n  initial begin\n    reset_fifo;\n    for(j=0;j<8;j=j+1) begin\n      for(i=0;i<8;i=i+1)put(8\'(j*8+i));\n      repeat(5) @(negedge si_clk);\n      if(!full) $fatal(1,"FULL_FLAGS");\n      for(i=0;i<8;i=i+1)get;\n      repeat(5) @(negedge so_clk);\n      if(!empty) $fatal(1,"EMPTY_FLAGS");\n    end\n    fork\n      begin for(integer k=0;k<96;k=k+1) put(8\'(k^8\'ha5));end\n      begin for(integer k=0;k<96;k=k+1) get;end\n    join\n    put(8\'hab);reset_fifo;put(8\'hde);get;\n    $display("DPRETET_GOLDEN_PASS reads=%0d",reads);$finish;\n  end\n  initial begin #100000;$fatal(1,"TIMEOUT");end\n  tb_checker checker_i (.*);\nendmodule\n';
export const DPRETET_CHECKER =
  'module tb_checker(input wire si_clk,so_clk,si_res_n,so_res_n,full,empty);\n always @(posedge si_clk) if(si_res_n) assert (!$isunknown(full)) else $fatal(1,"FULL_UNKNOWN");\n always @(posedge so_clk) if(so_res_n) assert (!$isunknown(empty)) else $fatal(1,"EMPTY_UNKNOWN");\nendmodule\n';
export const DPRETET_WRAPPER =
  'module TopModule\n\n    #(\n        parameter DSIZE = 8,\n        parameter ASIZE = 4,\n        parameter FALLTHROUGH = "TRUE" // First word fall-through without latency\n    )(\n        input  wire             wclk,\n        input  wire             wrst_n,\n        input  wire             winc,\n        input  wire [DSIZE-1:0] wdata,\n        output wire             wfull,\n        output wire             awfull,\n        input  wire             rclk,\n        input  wire             rrst_n,\n        input  wire             rinc,\n        output wire [DSIZE-1:0] rdata,\n        output wire             rempty,\n        output wire             arempty\n    );\nasync_fifo #( .DSIZE(DSIZE), .ASIZE(ASIZE), .FALLTHROUGH(FALLTHROUGH) ) dut_core (.*);\nendmodule\n';
export const AXIS_TB =
  '`timescale 1ns/1ps\nmodule tb;\n  reg clk=0,rst=1;\n  always #5 clk=~clk;\n  reg [7:0] s_axis_tdata=0;\n  reg s_axis_tvalid=0,s_axis_tlast=0,s_axis_tuser=0,m_axis_tready=0;\n  wire s_axis_tready,m_axis_tvalid,m_axis_tlast,m_axis_tuser;\n  wire [7:0] m_axis_tdata;\n  TopModule #(.DEPTH(8),.DATA_WIDTH(8),.KEEP_ENABLE(0),.LAST_ENABLE(1),.USER_ENABLE(1),.RAM_PIPELINE(1),.FRAME_FIFO(0)) dut(\n    .clk(clk),.rst(rst),.s_axis_tdata(s_axis_tdata),.s_axis_tkeep(1\'b1),\n    .s_axis_tvalid(s_axis_tvalid),.s_axis_tready(s_axis_tready),.s_axis_tlast(s_axis_tlast),\n    .s_axis_tid(8\'b0),.s_axis_tdest(8\'b0),.s_axis_tuser(s_axis_tuser),\n    .m_axis_tdata(m_axis_tdata),.m_axis_tvalid(m_axis_tvalid),.m_axis_tready(m_axis_tready),\n    .m_axis_tlast(m_axis_tlast),.m_axis_tuser(m_axis_tuser),.pause_req(1\'b0));\n  reg [9:0] queue[0:4095];\n  integer head=0,tail=0,reads=0,writes=0,stalls=0,i;\n  reg stalled=0, accepted=0;\n  reg [9:0] held;\n  always @(posedge clk) begin\n    accepted=s_axis_tvalid && s_axis_tready;\n    if(rst) begin head=0;tail=0;stalled=0;end\n    else begin\n      if(stalled && (!m_axis_tvalid || {m_axis_tuser,m_axis_tlast,m_axis_tdata} !== held)) $fatal(1,"BACKPRESSURE_STABILITY");\n      if(s_axis_tvalid && s_axis_tready) begin\n        queue[tail]={s_axis_tuser,s_axis_tlast,s_axis_tdata};tail=tail+1;writes=writes+1;\n      end\n      if(m_axis_tvalid && m_axis_tready) begin\n        if(head>=tail || {m_axis_tuser,m_axis_tlast,m_axis_tdata} !== queue[head]) $fatal(1,"FIFO_ORDER head=%0d",head);\n        head=head+1;reads=reads+1;\n      end\n      stalled=m_axis_tvalid && !m_axis_tready;\n      held={m_axis_tuser,m_axis_tlast,m_axis_tdata};\n      if(stalled) stalls=stalls+1;\n    end\n  end\n  task cycle(input bit offer,input bit ready);\n    begin\n      @(negedge clk);\n      if(!s_axis_tvalid || accepted) begin\n        s_axis_tvalid=offer;s_axis_tdata=8\'(writes*37+11);s_axis_tlast=(writes%5==4);s_axis_tuser=(writes%7==0);\n      end\n      m_axis_tready=ready;\n    end\n  endtask\n  initial begin\n    repeat(4) @(negedge clk);rst=0;\n    // Fill and stall, mixed throughput, repeated wraps, then flush queued data.\n    for(i=0;i<40;i=i+1)cycle(1,0);\n    for(i=0;i<700;i=i+1)cycle(i%7!=0,i%5!=0);\n    @(negedge clk);rst=1;s_axis_tvalid=0;m_axis_tready=0;\n    repeat(4) @(negedge clk);rst=0;\n    for(i=0;i<300;i=i+1)cycle(i%3!=0,i%4!=0);\n    cycle(0,1);\n    while(s_axis_tvalid) cycle(0,1);\n    repeat(30) @(negedge clk);\n    if(head!=tail || reads<300 || stalls<20) $fatal(1,"INSUFFICIENT_OR_UNDRAINED");\n    $display("AXIS_GOLDEN_PASS reads=%0d writes=%0d stalls=%0d",reads,writes,stalls);$finish;\n  end\n  initial begin #100000;$fatal(1,"TIMEOUT");end\n  tb_checker checker_i (.*);\nendmodule\n';
export const AXIS_CHECKER =
  'module tb_checker(input wire clk,rst,s_axis_tready,m_axis_tvalid);\n always @(posedge clk) if(!rst) assert (!$isunknown({s_axis_tready,m_axis_tvalid})) else $fatal(1,"HANDSHAKE_UNKNOWN");\nendmodule\n';
export const AXIS_WRAPPER =
  "module TopModule #\n(\n    // FIFO depth in words\n    // KEEP_WIDTH words per cycle if KEEP_ENABLE set\n    // Rounded up to nearest power of 2 cycles\n    parameter DEPTH = 4096,\n    // Width of AXI stream interfaces in bits\n    parameter DATA_WIDTH = 8,\n    // Propagate tkeep signal\n    // If disabled, tkeep assumed to be 1'b1\n    parameter KEEP_ENABLE = (DATA_WIDTH>8),\n    // tkeep signal width (words per cycle)\n    parameter KEEP_WIDTH = ((DATA_WIDTH+7)/8),\n    // Propagate tlast signal\n    parameter LAST_ENABLE = 1,\n    // Propagate tid signal\n    parameter ID_ENABLE = 0,\n    // tid signal width\n    parameter ID_WIDTH = 8,\n    // Propagate tdest signal\n    parameter DEST_ENABLE = 0,\n    // tdest signal width\n    parameter DEST_WIDTH = 8,\n    // Propagate tuser signal\n    parameter USER_ENABLE = 1,\n    // tuser signal width\n    parameter USER_WIDTH = 1,\n    // number of RAM pipeline registers\n    parameter RAM_PIPELINE = 1,\n    // use output FIFO\n    // When set, the RAM read enable and pipeline clock enables are removed\n    parameter OUTPUT_FIFO_ENABLE = 0,\n    // Frame FIFO mode - operate on frames instead of cycles\n    // When set, m_axis_tvalid will not be deasserted within a frame\n    // Requires LAST_ENABLE set\n    parameter FRAME_FIFO = 0,\n    // tuser value for bad frame marker\n    parameter USER_BAD_FRAME_VALUE = 1'b1,\n    // tuser mask for bad frame marker\n    parameter USER_BAD_FRAME_MASK = 1'b1,\n    // Drop frames larger than FIFO\n    // Requires FRAME_FIFO set\n    parameter DROP_OVERSIZE_FRAME = FRAME_FIFO,\n    // Drop frames marked bad\n    // Requires FRAME_FIFO and DROP_OVERSIZE_FRAME set\n    parameter DROP_BAD_FRAME = 0,\n    // Drop incoming frames when full\n    // When set, s_axis_tready is always asserted\n    // Requires FRAME_FIFO and DROP_OVERSIZE_FRAME set\n    parameter DROP_WHEN_FULL = 0,\n    // Mark incoming frames as bad frames when full\n    // When set, s_axis_tready is always asserted\n    // Requires FRAME_FIFO to be clear\n    parameter MARK_WHEN_FULL = 0,\n    // Enable pause request input\n    parameter PAUSE_ENABLE = 0,\n    // Pause between frames\n    parameter FRAME_PAUSE = FRAME_FIFO\n)\n(\n    input  wire                   clk,\n    input  wire                   rst,\n\n    /*\n     * AXI input\n     */\n    input  wire [DATA_WIDTH-1:0]  s_axis_tdata,\n    input  wire [KEEP_WIDTH-1:0]  s_axis_tkeep,\n    input  wire                   s_axis_tvalid,\n    output wire                   s_axis_tready,\n    input  wire                   s_axis_tlast,\n    input  wire [ID_WIDTH-1:0]    s_axis_tid,\n    input  wire [DEST_WIDTH-1:0]  s_axis_tdest,\n    input  wire [USER_WIDTH-1:0]  s_axis_tuser,\n\n    /*\n     * AXI output\n     */\n    output wire [DATA_WIDTH-1:0]  m_axis_tdata,\n    output wire [KEEP_WIDTH-1:0]  m_axis_tkeep,\n    output wire                   m_axis_tvalid,\n    input  wire                   m_axis_tready,\n    output wire                   m_axis_tlast,\n    output wire [ID_WIDTH-1:0]    m_axis_tid,\n    output wire [DEST_WIDTH-1:0]  m_axis_tdest,\n    output wire [USER_WIDTH-1:0]  m_axis_tuser,\n\n    /*\n     * Pause\n     */\n    input  wire                   pause_req,\n    output wire                   pause_ack,\n\n    /*\n     * Status\n     */\n    output wire [$clog2(DEPTH):0] status_depth,\n    output wire [$clog2(DEPTH):0] status_depth_commit,\n    output wire                   status_overflow,\n    output wire                   status_bad_frame,\n    output wire                   status_good_frame\n);\naxis_fifo #( .DEPTH(DEPTH), .DATA_WIDTH(DATA_WIDTH), .KEEP_ENABLE(KEEP_ENABLE), .KEEP_WIDTH(KEEP_WIDTH), .LAST_ENABLE(LAST_ENABLE), .ID_ENABLE(ID_ENABLE), .ID_WIDTH(ID_WIDTH), .DEST_ENABLE(DEST_ENABLE), .DEST_WIDTH(DEST_WIDTH), .USER_ENABLE(USER_ENABLE), .USER_WIDTH(USER_WIDTH), .RAM_PIPELINE(RAM_PIPELINE), .OUTPUT_FIFO_ENABLE(OUTPUT_FIFO_ENABLE), .FRAME_FIFO(FRAME_FIFO), .USER_BAD_FRAME_VALUE(USER_BAD_FRAME_VALUE), .USER_BAD_FRAME_MASK(USER_BAD_FRAME_MASK), .DROP_OVERSIZE_FRAME(DROP_OVERSIZE_FRAME), .DROP_BAD_FRAME(DROP_BAD_FRAME), .DROP_WHEN_FULL(DROP_WHEN_FULL), .MARK_WHEN_FULL(MARK_WHEN_FULL), .PAUSE_ENABLE(PAUSE_ENABLE), .FRAME_PAUSE(FRAME_PAUSE) ) dut_core (.*);\nendmodule\n";
